# Contributing

PhotoGit is pre-1.0. Open an issue before starting a large behavior or schema change. Keep changes narrowly scoped, add tests for merge and persistence behavior, and run `npm test` plus `npm run check` before opening a pull request.

Never add real artwork, credentials, access tokens, or private document metadata to fixtures.
